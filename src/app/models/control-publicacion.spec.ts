import { ControlPublicacion } from './control-publicacion';

describe('ControlPublicacion', () => {
  it('should create an instance', () => {
    expect(new ControlPublicacion()).toBeTruthy();
  });
});
